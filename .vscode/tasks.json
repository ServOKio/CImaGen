{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile windows debug",
            "command": "cd ${workspaceFolder}; flutter build windows --debug",
            "type": "shell"
        }
    ]
}